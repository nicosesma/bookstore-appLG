extends ../layout.pug

block content

  h1 All Books
  table
    thead
      tr
        th ID
        th Title
    tbody
      for book in books
        tr
          td= book.id
          td
            a(href="/books/"+book.id)= book.title
